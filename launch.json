{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "R-Debugger",
            "name": "Debug R File",
            "request": "launch",
            "workingDirectory": "${workspaceFolder}/R",
            "debugMode": "file",
            "allowGlobalDebugging": false
        }
    ]
}